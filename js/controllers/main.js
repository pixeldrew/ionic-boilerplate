/*
 * add controllers to the define statement below so they are automatically loaded by boot
 * define(["./awesome"], function() {});
 *
 * controller definitions should require needed dependencies (ie, if a controller uses a particular
 * directive/service/whatever, it should reference it in it's opening define statement)
 */
define(['./home'], function() {});